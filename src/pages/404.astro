---
import BaseLayout from "../layouts/BaseLayout.astro";
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="Página não encontrada">
    <div
        class="min-h-[60vh] flex flex-col items-center justify-center text-center px-6"
    >
        <h1
            class="font-display text-9xl font-bold tracking-tighter text-primary animate-pulse mb-8"
        >
            404
        </h1>
        <h2
            class="font-display text-3xl font-bold uppercase tracking-widest mb-4"
        >
            Artefato Desaparecido
        </h2>
        <p class="text-muted-foreground max-w-md mb-12">
            O meme que você está procurando parece ter sido deletado da
            existência ou nunca foi catalogado em nossos arquivos históricos.
        </p>
        <a
            href={`${base}`}
            class="bg-primary text-white font-bold py-4 px-10 uppercase tracking-[0.2em] text-[10px] hover:bg-primary/90 transition-all"
        >
            Voltar para o Início
        </a>
    </div>
</BaseLayout>
